package data.connection;

import java.sql.Connection;
import java.sql.SQLException;

// Apenas um placeholder. A persistência real está no HistoricoDAO.
public class ConexaoMySQL {

    // Método para simular a conexão (futuramente usará JDBC)
    public static Connection conectar() throws SQLException {
        throw new UnsupportedOperationException("Conexão MySQL não implementada nesta base.");
    }
}